<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context=".AdminDashboardActivity">

    <!-- Encabezado tipo "hero" con el color navy del proyecto -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/header_admin"
        android:layout_width="0dp"
        android:layout_height="140dp"
        android:background="@color/navy_dark"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:id="@+id/iv_admin_shield"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginStart="24dp"
            android:layout_marginTop="28dp"
            android:src="@drawable/ic_admin_shield"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            tools:ignore="ContentDescription" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:text="@string/admin_dashboard_title"
            android:textColor="@color/white"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintTop_toTopOf="@id/iv_admin_shield"
            app:layout_constraintBottom_toBottomOf="@id/iv_admin_shield"
            app:layout_constraintStart_toEndOf="@id/iv_admin_shield" />

        <ImageView
            android:id="@+id/btn_admin_logout"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginEnd="24dp"
            android:src="@drawable/ic_signout_arrow"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:layout_constraintTop_toTopOf="@id/iv_admin_shield"
            app:layout_constraintBottom_toBottomOf="@id/iv_admin_shield"
            app:layout_constraintEnd_toEndOf="parent"
            tools:ignore="ContentDescription" />

        <TextView
            android:id="@+id/tv_admin_welcome"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="24dp"
            android:layout_marginTop="16dp"
            android:layout_marginEnd="24dp"
            android:textColor="@color/white"
            android:alpha="0.85"
            android:textSize="13sp"
            tools:text="Bienvenido, admin@stylebyte.com"
            app:layout_constraintTop_toBottomOf="@id/iv_admin_shield"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <androidx.core.widget.NestedScrollView
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/header_admin"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Fila 1: Productos / Categorías -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <include android:id="@+id/card_admin_products" layout="@layout/item_admin_dashboard_card" />
                <include android:id="@+id/card_admin_categories" layout="@layout/item_admin_dashboard_card" />
            </LinearLayout>

            <!-- Fila 2: Pedidos / Usuarios -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:orientation="horizontal">

                <include android:id="@+id/card_admin_orders" layout="@layout/item_admin_dashboard_card" />
                <include android:id="@+id/card_admin_users" layout="@layout/item_admin_dashboard_card" />
            </LinearLayout>

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>
